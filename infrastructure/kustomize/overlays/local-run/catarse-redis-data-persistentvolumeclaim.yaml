apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: catarse-redis-data
  name: catarse-redis-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
