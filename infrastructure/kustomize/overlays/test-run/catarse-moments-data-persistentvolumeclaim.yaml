apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: catarse-moments-data
  name: catarse-moments-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
