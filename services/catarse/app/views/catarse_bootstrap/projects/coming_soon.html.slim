- content_for :title, "#{@project.name} · #{CatarseSettings[:company_name]}"
= render 'layouts/include_redactor_assets'

- parameters = { project_id: @project.id, id: @project.id, user_id: @project.user_id, project_state: @project.state, mode: @project.mode, rails_errors: @project.errors.to_json }.to_json

#application data-parameters=parameters

- content_for :application_js do
  = render 'layouts/include_catarsejs'
  / = TODO: check if needs jquery-ui for this screen
