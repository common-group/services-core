- content_for :title, "Saldo"
- content_for :application_js do
  = render 'layouts/include_catarsejs'

= render 'shared/header_big'

#user-balance-root data-mithril="UsersBalance" data-parameters={'user_id': resource.id}.to_json

= render 'shared/footer'

